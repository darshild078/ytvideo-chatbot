# Server Configuration
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# MongoDB (Required)
# Setup: https://www.mongodb.com/cloud/atlas/register
# 1. Create free M0 cluster
# 2. Database Access → Add user
# 3. Network Access → Add 0.0.0.0/0
# 4. Connect → Get connection string
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/youtube-rag?retryWrites=true&w=majority

# Pinecone Vector DB (Required)
# Setup: https://app.pinecone.io/
# 1. Create free Starter project
# 2. Create index: name=youtube-transcripts, dimensions=384, metric=cosine
# 3. Copy API key from dashboard
PINECONE_API_KEY=your_pinecone_api_key
PINECONE_INDEX_NAME=youtube-transcripts

# Groq API (Required - FREE, no credit card)
# Setup: https://console.groq.com/
# 1. Sign up (no credit card)
# 2. API Keys → Create new key (starts with gsk_)
GROQ_API_KEY=gsk_your_groq_api_key

# Redis (Required for job queue)
# Local: Install Redis (brew install redis / apt-get install redis-server)
# Cloud: Use Upstash free tier (https://upstash.com/)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Python Microservice URL
PYTHON_SERVICE_URL=http://localhost:8000

# YouTube Data API (Optional - for metadata only)
YOUTUBE_API_KEY=
